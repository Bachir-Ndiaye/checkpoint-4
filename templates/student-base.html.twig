{% extends 'base.html.twig' %}

{% block title %}Espace Eleve - {{ app.user.firstname }}{% endblock %}

{% block aside %}
    <aside>
        <h1>Bonjour, {{ app.user.Lastname }}</h1>
        <div class="fixed-board">
            <a href="{{ path('profile_index', {'id': app.user.getId}) }}">
                <i class="fas fa-home"></i>
                Accueil
            </a>
            <a href="{{ path('profile_media_student', {'id': app.user.getId}) }}">
                <i class="fas fa-file"></i>
                Mes documents
            </a>
            <a href="{{ path('profile_collegues', {'id': app.user.getId}) }}">
                <i class="fas fa-child"></i>
                Mes collègues
            </a>
            <a href="{{ path('profile_notice', {'id': app.user.getId}) }}">
                <i class="fas fa-chart-bar"></i>
                Noter son prof
            </a>
            <a href="{{ path('profile_send', {'id': app.user.getId}) }}">
                <i class="fas fa-envelope-square"></i>
                Messagerie
            </a>
            <a href="{{ path('app_logout') }}">
                <i class="fas fa-sign-out-alt"></i>
                Déconnexion
            </a>
        </div>
    </aside>
{% endblock %}
{% block body %}

{% endblock %}
