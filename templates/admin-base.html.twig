{% extends 'base.html.twig' %}

{%  block aside %}
    <aside>
        <h1>Bonjour, {{ app.user.Lastname }}</h1>
        <div class="fixed-board">
            <a href="{{ path('profile_index', {'id': app.user.getId}) }}">
                <i class="fas fa-home"></i>
                Accueil
            </a>
            <a href="{{ path('profile_students', {'id': app.user.getId}) }}">
                <i class="fas fa-child"></i>
                Mes èleves
            </a>
            <a href="{{ path('profile_media', {'id': app.user.getId}) }}">
                <i class="fas fa-paper-plane" aria-hidden="true"></i>
                Envoyer un média
            </a>
            <a href="{{ path('profile_notes', {'id': app.user.getId}) }}">
                <i class="fas fa-file"></i>
                Notes reçus
            </a>
            <a href="{{ path('profile_send', {'id': app.user.getId}) }}">
                <i class="fas fa-envelope-square"></i>
                Messagerie
            </a>
            <a href="{{ path('app_logout') }}">
                <i class="fas fa-sign-out-alt"></i>
                Déconnexion
            </a>

        </div>
    </aside>
{% endblock %}
